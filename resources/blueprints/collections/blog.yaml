tabs:
  main:
    label: Main
    fields:
      - handle: title
        field:
          type: text
          required: true

      - handle: slug
        field:
          type: slug
          required: true

      - handle: content
        field:
          type: tiptap
